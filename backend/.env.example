# Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# MongoDB Configuration
# Using MongoDB Atlas (Cloud)
MONGODB_URI=mongodb+srv://raseshvarshney82_db_user:YOUR_NEW_PASSWORD@cluster0.s40dbqy.mongodb.net/portfolio?retryWrites=true&w=majority

# Local MongoDB (if running locally)
# MONGODB_URI=mongodb://localhost:27017/portfolio

# Email Configuration (Gmail)
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_new_app_password

# Note: For Gmail, you need to:
# 1. Enable 2-Factor Authentication
# 2. Create an App Password (NOT your regular password)
# 3. Use the App Password in EMAIL_PASSWORD
# Visit: https://myaccount.google.com/apppasswords

# Optional: API Keys for future features
# OPENAI_API_KEY=sk-...
# STRIPE_API_KEY=sk_test_...

